training:
  epochs: 40
  batch_size: 8
  learning_rate: 0.0004
  weight_decay: 0.0001
  grad_clip: 1.0
  init_weights: null
  deterministic: false

model:
  dropout: 0.2
  lstm_hidden: 512
  freeze_backbone: false
  init_backbone: null

data:
  dataset: "pjm"
  train_dir: "extracted"
  dev_dir: "extracted"
  annotation_dir: "./ann_annotated_glosses"
  train_ann: "annotations/PJM_gloss.train.txt"
  dev_ann: "annotations/PJM_gloss.dev.txt"
  num_workers: 8
  pin_memory: true
  min_sentences: 4
  train_ratio: 0.75

optimizer:
  milestones: [20, 35]
  gamma: 0.1

system:
  checkpoint_dir: "checkpoints"
  device: "auto"

logging:
  tensorboard: true
  wandb: false
  log_dir: "runs"
  log_interval: 50
  histogram_interval: 5
